# Environment for the model and the paper experiments.
# Core versions captured from the host that produced the results.
python>=3.11,<3.13
numpy==2.4.4
scipy==1.17.1
pandas
matplotlib

# Preprocessing and map-matching only (these stages need the raw GPS,
# which is not shipped in this repository).
pyarrow
leuvenmapmatching
